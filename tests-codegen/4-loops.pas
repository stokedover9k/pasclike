program vars_test;

var
   a, b, c, d, i, j		: integer;
   nums				: array[1..10] of array[1..9] of integer;
   bob				: record
				     age    : integer;
				     name   : string;
				     grades : array[1..10] of integer
				  end;	    
   correct			: boolean;

function divides(x, y : integer) : boolean;
begin

end; { divides }

procedure gamble(m, n : integer); forward;
		      
begin

   while ( i div j > b + c * d ) do
   begin
      a := nums[c][d];
   end;

   while ( divides(a, b + c) ) do
   begin
      a := nums[c][d];
   end;

   for a := nums[j][j] to nums[i][i] do
   begin
      a := nums[c][d];
   end;

end.
